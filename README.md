# Jessey Clinic Pharmacy Management System

Complete pharmacy system with real-time stock, expiry tracking, sales, reports, and audit logs.

## Installation

```bash
python -m venv venv
source venv/bin/activate  # or venv\Scripts\activate on Windows
pip install -r requirements.txt
python app.py